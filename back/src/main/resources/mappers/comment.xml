<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.pathpartner.reviewarticle.repository.CommentDao">
    <select id="listComment" parameterType="String" resultType="">
        select c.content, c.creation_date, u.nickname, c.uuid
        from comment c
        join user u where c.uuid=u.uuid
        where c.review_article_id = #{reviewArticleId}
    </select>

    <select id="getComment" parameterType="String" resultType="commentDto">
        select comment_id, content, creation_date, uuid, review_article_id
        from comment
        where comment_id = #{commentId}
    </select>

    <insert id="writeComment" parameterType="commentDto">
        insert into comment(comment_id, content, creation_date, uuid, review_article_id)
        values(UUID(),#{content},now(),#{uuid},#{reviewArticleId})
    </insert>

    <insert id="editComment" parameterType="commentDto">
        update comment
        set content = #{content}
        where comment_id = #{commentId}
    </insert>

    <delete id="deleteComment" parameterType="String">
        delete from comment
        where comment_id = #{commentId}
    </delete>

    <select id="getTotalCommentCount" resultType="int">
        select count(*) from comment
    </select>
</mapper>