<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.enjoytrip.board.repository.CommentDao">

    <resultMap type="commentDto" id="comment">
        <result column="comment_id" property="commentNo"/>
        <result column="article_no" property="articleNo"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="content" property="content"/>
        <result column="register_time" property="registerTime"/>
    </resultMap>

    <insert id="writeComment" parameterType="commentDto">
        insert into comment (article_no, user_id, user_name, content, register_time)
        values (#{articleNo}, #{userId}, #{userName}, #{content}, now())
        <selectKey resultType="int" keyProperty="commentNo" order="AFTER">
            select last_insert_id()
        </selectKey>
    </insert>

    <select id="listComment" parameterType="int" resultMap="comment">
        select comment_id, article_no, user_id, user_name, content, register_time
        from comment
        where article_no = #{articleNo}
        order by register_time desc
    </select>

    <delete id="deleteComment" parameterType="int">
        delete from comment
        where comment_id = #{commentNo}
    </delete>


</mapper>